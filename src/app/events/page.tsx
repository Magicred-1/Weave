import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Leaderboard",
};

const EventPage = () => {
    return (
        <div>
        <h1>Event Page</h1>
        </div>
    );
}

export default EventPage;