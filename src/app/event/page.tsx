import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Events",
};

const EventPage = () => {
    return (
        <div>
        <h1>Event Page</h1>
        </div>
    );
}

export default EventPage;